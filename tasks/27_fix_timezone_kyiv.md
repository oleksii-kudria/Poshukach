# 🧩 Завдання 27 — Врахування таймзони Europe/Kyiv при формуванні дат і часу

## 🎯 Мета
Виправити часовий зсув у виведених полях `firstDate` та `lastDate` (а також похідних, як `firstDateEpoch` та `lastDateEpoch`), які наразі відображають час **на 2 години менше**, ніж фактичний локальний час в Україні.

---

## ⚙️ Проблема
Під час обробки даних із DHCP або RDS (`deviceTime`, `Время повторения` тощо) час конвертується у формат `YYYY.MM.DD HH:MM` без урахування таймзони.  
Результат відстає на 2 години від локального (Europe/Kyiv, UTC+2 або UTC+3 у літній період).

---

## 🧩 Завдання
1. Усі конверсії часу (`deviceTime`, `firstDateEpoch`, `lastDateEpoch`) повинні виконуватись із врахуванням **таймзони “Europe/Kyiv”**.
2. Для цього використати стандартну бібліотеку Python `zoneinfo`:
   ```python
   from zoneinfo import ZoneInfo
   dt = datetime.fromtimestamp(epoch / 1000, tz=ZoneInfo("Europe/Kyiv"))
   formatted = dt.strftime("%Y.%m.%d %H:%M")
   ```
3. У випадку, якщо `deviceTime` не містить інформацію про таймзону, **вважати його значення UTC**, і потім **переводити у “Europe/Kyiv”**.
4. Оновити всі місця, де виконується:
   - Конвертація epoch → datetime
   - Формування `firstDate` / `lastDate`
   - Формування часу у звітах (`data/report/*.txt`)
5. Не змінювати самі epoch-значення (`firstDateEpoch`, `lastDateEpoch`), лише **людський формат** часу.

---

## 🧪 Тест
Вхідне значення:
```
deviceTime = 1755006240482  # відповідає 2025-10-12 10:24:00 UTC
```
Очікувано:
```
2025.10.12 12:24  (Europe/Kyiv, +2h)
```

---

## 🖨️ Очікуваний вивід
```
✅ Часові дані приведено до таймзони Europe/Kyiv
```

---

## ✅ Критерії прийняття
- [ ] Всі дати у CSV та звітах відповідають локальному київському часу.
- [ ] Епох-значення не змінені.
- [ ] Різниця з фактичним часом не перевищує ±1 хвилину.
