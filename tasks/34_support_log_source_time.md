# 🧩 Завдання 34 — Підтримка `Log Source Time` як альтернативи `deviceTime`

## 🎯 Мета
Забезпечити коректну обробку файлів `data/raw/dhcp/*.csv`, у яких **відсутній стовпчик `deviceTime`**, але присутній альтернативний часовий стовпчик **`Log Source Time`**.

Подальша логіка обробки (агрегація, пайплайни, фільтрація) **не змінюється**.

---

## ⚙️ Вимоги

### 1) Перевірка наявності часового стовпчика
Під час валідації заголовків CSV:

1. Спочатку перевірити наявність стовпчика:
   - `deviceTime`
2. Якщо `deviceTime` **відсутній**, перевірити наявність:
   - `Log Source Time`
3. Якщо **обидва стовпчики відсутні**:
   - вивести помилку
   ```
   ❌ Відсутні часові поля deviceTime та Log Source Time у файлі <filename>
   ```
   - **зупинити виконання скрипту**

---

## 🕒 Формат `Log Source Time`

### Приклад значення:
```
Dec 31, 2025, 1:32:58 PM
```

### Особливості:
- текстовий формат дати/часу
- англійська локаль
- 12‑годинний формат з `AM / PM`
- відсутня явна часова зона

---

## 🔁 Конвертація в epoch

### Вимоги:
- значення `Log Source Time` необхідно:
  1. розпарсити у datetime
  2. інтерпретувати як **Europe/Kyiv**
  3. перевести у **epoch (seconds або milliseconds — відповідно до поточної реалізації)**

### Рекомендований формат парсингу:
```
%b %d, %Y, %I:%M:%S %p
```

### Результат:
- отримане epoch‑значення **використовується так само**, як `deviceTime`:
  - `firstDateEpoch`
  - `lastDateEpoch`
  - `dateList`
  - обчислення `firstDate / lastDate`

---

## 🔄 Подальша логіка
Після нормалізації часу:
- увесь подальший пайплайн працює **без змін**
- не повинно бути різниці між:
  - оригінальним `deviceTime`
  - або значенням, отриманим з `Log Source Time`

---

## 🧪 Тести

### Випадок 1 — `deviceTime` присутній
- `Log Source Time` ігнорується
- поведінка без змін

### Випадок 2 — `deviceTime` відсутній, `Log Source Time` присутній
```
Log Source Time = Dec 31, 2025, 1:32:58 PM
```
Очікування:
- значення коректно конвертоване в epoch
- агрегація працює штатно

### Випадок 3 — обидва поля відсутні
Очікування:
- вивід помилки
- зупинка виконання

---

## ✅ Критерії прийняття
- [ ] Підтримується `Log Source Time` як fallback
- [ ] Час коректно парситься та конвертується в epoch
- [ ] Враховується часова зона Europe/Kyiv
- [ ] Поточна логіка обробки не змінена
- [ ] Помилка при відсутності обох полів блокує виконання
