# 🧩 Завдання 35 — Альтернативні назви стовпчиків у raw DHCP CSV (режим `Log Source Time` + авто-визначення `payloadAsUTF`)

## 🎯 Мета
Підтримати обробку файлів `data/raw/dhcp/*.csv`, у яких **відсутній** стовпчик `deviceTime`, але присутній `Log Source Time` (див. Завдання 34).

У таких файлах **відрізняються назви стовпчиків** від стандартних, а також **немає фіксованої назви** стовпчика для `payloadAsUTF`.

Потрібно:
- правильно мапити альтернативні назви для `logSourceIdentifier` та `sourcMACAddress`
- автоматично визначати, який стовпчик є `payloadAsUTF` за вмістом (містить `dhcp,info`)
- ігнорувати всі інші стовпчики (у т.ч. без назви)
- подальша логіка пайплайна **без змін**

---

## ⚙️ Умова застосування
Ця логіка активується **лише якщо** для конкретного файлу виконується умова:
- `deviceTime` **відсутній**
- `Log Source Time` **присутній**

Для «звичайних» DHCP CSV (де є `deviceTime`) — поведінка **без змін**.

---

## 🗂️ Мапінг стовпчиків для цього типу файлів

### 1) `logSourceIdentifier`
Стандартне поле: `logSourceIdentifier`  
У цьому типі файлів: **`Log Source Identifier`**

Отже:
- `logSourceIdentifier` ← значення зі стовпчика `"Log Source Identifier"`

### 2) `sourcMACAddress`
Стандартне поле: `sourcMACAddress`  
У цьому типі файлів: **`Source MAC`**

Отже:
- `sourcMACAddress` ← значення зі стовпчика `"Source MAC"`

### 3) `deviceTime`
- у цьому типі файлів відсутній
- замість нього використовується `"Log Source Time"` (парсинг та epoch — згідно Завдання 34)

### 4) `payloadAsUTF` (назва стовпчика відсутня / невідома)
У цьому типі файлів **немає гарантованої назви** стовпчика, який відповідає `payloadAsUTF`.

Потрібно **автоматично визначити** цей стовпчик так:

- знайти стовпчик, значення якого **містить підрядок**:
  - `dhcp,info`
- цей стовпчик вважати `payloadAsUTF`

> Важливо: перевірка має бути нечутлива до регістру (case-insensitive).  
> Достатньо знайти перший стовпчик, який містить `dhcp,info` хоча б в одному рядку.

---

## 🔎 Алгоритм визначення `payloadAsUTF`
1. Отримати список усіх стовпчиків у файлі (включно з неіменованими/порожніми назвами, якщо такі присутні).
2. Ітерувати по стовпчиках і перевірити вибірку значень (наприклад перші N рядків або всі рядки) на наявність `dhcp,info`.
3. Перший стовпчик, де знайдено `dhcp,info`, вважати `payloadAsUTF`.
4. Якщо жоден стовпчик не містить `dhcp,info`:
   - вивести помилку
   ```
   ❌ Не вдалося визначити payloadAsUTF: жоден стовпчик не містить "dhcp,info" у файлі <filename>
   ```
   - зупинити виконання скрипту

---

## 🚫 Інші стовпчики
- Будь-які додаткові стовпчики **не використовувати**
- Файл може містити:
  - додаткові колонки з іменем
  - додаткові колонки без імені
  - службові/порожні колонки
- Вони не повинні впливати на обробку

---

## 🧾 Логування (коротко, українською)
Для файлів цього типу додати стислий лог у консоль:
```
🔧 Файл <filename>: використано Log Source Time, мапінг полів застосовано, payloadAsUTF визначено як стовпчик "<column_name>"
```

---

## 🧪 Тести

### Випадок 1 — альтернативний файл
- `deviceTime` відсутній
- `Log Source Time` присутній
- `"Log Source Identifier"` та `"Source MAC"` присутні
- один зі стовпчиків містить `dhcp,info …`
Очікування:
- файл обробляється успішно
- `payloadAsUTF` береться з визначеного стовпчика
- час конвертується згідно Завдання 34

### Випадок 2 — `dhcp,info` не знайдено
Очікування:
- помилка визначення `payloadAsUTF`
- зупинка виконання

---

## ✅ Критерії прийняття
- [ ] Логіка активується лише для файлів з `Log Source Time` і без `deviceTime`
- [ ] Підтримано мапінг: `Log Source Identifier` → `logSourceIdentifier`
- [ ] Підтримано мапінг: `Source MAC` → `sourcMACAddress`
- [ ] `payloadAsUTF` визначається за наявністю `dhcp,info` у значеннях стовпчика
- [ ] Інші стовпчики ігноруються (в т.ч. без назви)
- [ ] Подальша логіка пайплайна без змін
