# 🧩 Завдання 18 — Очищення директорії `data/result/` перед запуском обробки

## 🎯 Мета
Забезпечити, щоб перед кожним запуском скрипту (`python3 scripts/psh.py run` або будь-якої іншої основної команди обробки)  
старі результати не впливали на нові — тобто **видаляти всі попередні `.csv` файли** з директорії `data/result/`,  
крім тестових або прикладних (`*.example.csv`).

---

## ⚙️ Логіка роботи

1. **Перед початком будь-якої обробки (потік A або B)** перевірити існування директорії:
   ```
   data/result/
   ```
   Якщо директорія відсутня — створити її.

2. **Видалити всі файли** з розширенням `.csv`,  
   які **не** закінчуються на `.example.csv`.

3. Для файлів, які **не вдалося видалити** (наприклад, через блокування у файловій системі) —  
   вивести попередження у консоль, але не переривати виконання скрипту.

---

## 📋 Приклад поведінки

Перед запуском:
```
data/result/
 ├── dhcp-true.csv
 ├── dhcp-false.csv
 ├── dhcp-ignore.csv
 ├── dhcp-network.csv
 ├── example.csv
```

Після очищення:
```
data/result/
 ├── example.csv
```

---

## 📊 Логування в консоль

```
🧹 Cleaning up result directory...
   • Видалено 4 файли з data/result/
   • Пропущено 1 файл (*.example.csv)
✅ Директорія очищена успішно.
```

---

## 🧱 Вимоги до реалізації
- Використовувати **відносні шляхи** (`data/result/`).
- Ігнорувати файли, що закінчуються на `.example.csv`.
- Якщо директорія `data/result/` не існує — створити її автоматично.
- Якщо немає жодного файлу для видалення — просто вивести:
  ```
  🧹 Немає файлів для очищення.
  ```

---

## ✅ Критерії прийняття
- [ ] Під час запуску будь-якої основної операції (`run`, `collect`, `report` тощо) виконується очищення результатів.
- [ ] Всі файли `*.csv`, окрім `*.example.csv`, видаляються.
- [ ] У консоль виводиться зрозуміла статистика про кількість видалених файлів.
- [ ] Відсутність помилок у разі відсутності директорії або файлів.
